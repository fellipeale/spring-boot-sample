#Runbook

##Prerequisites
* Maven
* Node.js

##Installation
Run those commands in the terminal:

    mvn install

    npm install

##Running
Run those commands in the terminal:

    mvn spring-boot:run &

    npm start &

Go to http://localhost:3000/
